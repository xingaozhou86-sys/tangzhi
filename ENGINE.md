# Reframe / 穿画 — 引擎文档

> **2026-09-01 方向推翻**：玩家试玩判定连线解谜"根本就不好玩儿"，要求对标画中世界（Gorogoa）的跨画跳跃趣味。连线引擎（`model.ts` 五章十八关）整体下架，游戏改为 **《穿画 · Passage》**——见第 0 节。旧引擎文件保留作参考（`model.ts` / 旧测试仍全绿），但不再被 App 引用。

---

## 0. 穿画 · Passage（当前游戏）

设计句：**你是一粒光，住在画廊的画里；从一幅画，走进另一幅画。**

三个核心动作（对标 Gorogoa 的空间魔术，不抄内容）：

1. **入画** — 点按墙上一幅画，走进去（变焦全景）。画里还挂着更小的画，可以再进一层。
2. **拽出** — 把画里的小画拖到墙上，它变成一幅大画（Gorogoa 的"取出画框"）。
3. **咬合** — 把一幅画拖到另一幅画上：路接上路（link，两画并排、接口节点搭金线）、月亮落进灯里（overlay，只置旗标、场景自己变形）。咬不上会摇头+低音拒绝。

目标：让光从起点走到目标（金菱）。光永远实时显示"现在能走到哪"（可达=金线亮芯，断路=暗虚线），过关时光真的沿路线走一遍 → 铃 → 盖章「通」。

- 引擎：`src/game/world.ts`（纯数据+纯逻辑：场景/节点/边/咬合/旗标，BFS 可达与路线）。测试 `npm run test:world`（11 断言：三幕全部"初始不可解 → 指定操作后可解"，L3 验证"只咬合不拽出不可解"）。
- 界面：`src/game/WorldView.tsx`（墙/拖拽/变焦/画中画/光行走动画）。
- 切片三幕：**岸**（断桥接河，教咬合）、**借月**（月亮落进灯，教 overlay 变形）、**谷**（画中画，教入画+拽出）。
- 深链：`#/play/N`、`#/select`。存档 `passage-progress-v1`。

---

# 旧引擎存档（连线解谜 · 已下架）

> 设计句：A puzzle game where the player advances by revising what counts as an obstacle, a group, and a path.

## 1. 唯一恒真的规则

引擎里只有一条物理定律（`computeFlow`）：

> 两个端口相贴时连通 ⇔ **颜色相交，或任一侧边缘上有滤片端口**。

注意滤片是**按边缘**生效的：它只让"被它盖住的边缘"不计颜色（滤片自身有端口的那几条边）。所以滤片的朝向也是信息——第 7 关利用的就是这一点。

玩家在游戏里学到的所有其他"规则"（同色相连、完整轮廓才是物体、对称即正确、深色是障碍、重复是装饰……）都不是引擎规则，而是**关卡设计出来的错觉**。引擎从不背叛玩家，玩家背叛的是自己建立的分类框架。

## 2. 概念映射表

| 设计概念 | 引擎构造 | 字段 | 首次出现 |
|---|---|---|---|
| 图形归组（颜色/轮廓） | 端口 + 颜色 | `ports: [{dir, color}]` | 第 1 关 |
| 误读的规则（同色陷阱） | 偏色端口制造断口 | 颜色不匹配即断开 | 第 2 关 |
| 重新分类（叠合） | 滤片：被盖住的边缘不再校验颜色 | `isFilter` | 第 2 关 |
| 滤片按边缘生效 | 两片固定朝向的滤片二选一（竖缝/横缝），选错方向等于没盖 | `isFilter` 无 `rotatable` | 第 2 关 |
| 双色双读 | 独立时是物体、叠合时是滤片 | `dual` + `isFilter` | 第 3 关 |
| 负空间路径 | 叠有滤片时才生效的连接 | `hiddenLinks` | 第 3 关 |
| 裂缝（断口的可视预告） | 负空间未显影时画出半截残端 | 渲染层 `STUB` | 第 5 关 |
| 完整轮廓的错觉 | 封闭圆环装饰 + 断头线（无 link） | `art: 9/10` + 空 `links` | 第 4 关 |
| 不对称的接口 | 朝向即信息；不规则端口画成三角 | `rotatable` + `quirkyTab` | 第 9 关 |
| 滤片的朝向 | 滤片可旋转，只覆盖被盖住的边缘 | `isFilter` + `rotatable` | 第 7 关 |
| 深色不是障碍 | 可移动的遮片；压住时连接失效 | `isCover` + `coveredLinks` | 第 11 关 |
| 遮片即窗口 | 自带 link 的遮片：纸色通道穿透深色 | `isCover` + `links` | 第 12 关 |
| 改变条件/增加资源 | 画面边界可以拉开 | `expandCols` | 第 15 关 |
| 重复纹样的偏差 | 同 art 变体、不同端口朝向的砖 | 相同 `art` + 不同 `ports` | 第 16 关 |
| 压力—价值—行动（母题） | 最难拼的碎片上的金色纹样 | `gold` | 每关至少一块 |
| 入口/出口 | 棋盘边缘的终端，带颜色 | `entry` / `exit: Terminal` | 每关 |

## 3. 五章十八关

每章一个小循环：**教会 → 背叛 → 变体巩固 → 复合**。

| 关 | 章 | 名称 | 误读 → 重新分类 |
|---|---|---|---|
| 1 | 颜色 | 配色分类 | （建立：同色相连） |
| 2 | 颜色 | 色彩陷阱 | 同色永远对 → 滤片不计颜色 |
| 3 | 颜色 | 双色双读 | 碎片是物体 → 碎片可读作滤片 |
| 4 | 轮廓 | 完整轮廓 | 完整才是物体 → 完整也会是死路 |
| 5 | 轮廓 | 裂缝 | 断了就是坏了 → 裂缝是通道（残端预告负空间） |
| 6 | 轮廓 | 负空间拼接 | 空白是缺陷 → 空白等着显影 |
| 7 | 轮廓 | 两处断口 | 一片滤片不够 → 两种断口各一片，滤片朝向也是信息 |
| 8 | 朝向 | 转向 | （建立：点按旋转） |
| 9 | 朝向 | 不对称的接口 | 平衡即完成 → 不规则的朝向就是接口 |
| 10 | 朝向 | 朝向与颜色 | 单手不够 → 旋转 + 滤片复合 |
| 11 | 阴影 | 深色遮片 | 深色是障碍 → 深色可以挪开 |
| 12 | 阴影 | 窗口 | 遮片只能挪开 → 遮片可以换成窗口 |
| 13 | 阴影 | 两片深色 | 两片一样 → 一片要挪开、一片要替换 |
| 14 | 阴影 | 暗处与负空间 | "没有"只有一种 → 暗处要挪、空白要显影 |
| 15 | 边界 | 资源不足 | 边界固定 → 拉开边界制造空间 |
| 16 | 边界 | 重复纹样 | 重复是装饰 → 偏差组成路径 |
| 17 | 边界 | 拉开之后 | 改变条件就够了 → 还要再重新分类一次 |
| 18 | 边界 | 金色合拢 | 终关：旋转 + 遮片 + 显影，三件事全做 |

### 体验层（2026-09 重做：玩家试玩反馈"不知道在干嘛、过关没成就感"后推翻叙事层）

剧情层（无名画家/退稿/序章/题词/终幕 punchline）已**整体移除**（`story.ts` 删除），游戏只保留画廊视觉壳。取而代之的是一套"看得见自己在干嘛"的体验系统：

- **光永远实时流动**：`computeFlow` 输出的 `lit` 本就是"从入口可达"的全部节点，不分是否过关。点亮的线不再是淡光晕——主线芯变真金（`#e9b83a`）+ 一道亮芯（`#ffe9a8`，`.lit-core`），跨格桥与端子同步灌金。光流到哪里、死在哪里，永远看得见。
- **端子即目标**：入口是呼吸的金色光源菱（`.src-gem`，"光从这里来"），出口是空心目标菱（`.snk-gem`，到达后灌金），旁边的小色片声明所需颜色。
- **过关 = 盖章**：金光流完全程（`550 + maxDist × 110`ms）后，朱砂印「通」砸在画框右下角（`.seal-stamp` + `sfx.stamp()` + 画框轻顿），再隔 780ms 升起极简过关卡（只报"第 N 幅 · X 步"）。
- **新手引导**：L1 未动过时循环播放拖拽示范手势（`.teach-ghost`，CSS 变量驱动两格间动画）+ 气泡"按住一块画，拖到另一块上交换"；场上首次出现可旋转普通画时气泡提示"点按画面，可以旋转朝向"。第一步之后全部消失。
- **目录 = 展品墙**：每关一个小画框缩略图（优先 gold 砖），未解锁灰化上锁，通关灌金描边——成就感回流到一面逐渐点亮的墙。
- **深链自查**：`#/play/N` 直接进第 N 关，`#/select` 直接进展品墙（截图/调试用）。

### 演出层

让"连通"成为一场看得见的演出，而不是状态翻转：

- **金光传播**：`computeFlow` 在并查集之外同步记录连通边，从 `SRC` 做 BFS 输出 `dist`（跳数）与 `maxDist`。渲染层把每跳换算成 110ms 延迟（`LIGHT_STEP`），线段的金色辉光、跨格桥、出入口端子按各自距离 stagger 点亮——金光真的"流"过画面。
- **落子动画**：tile 的 React key 含格号（`id@cell`），换位即重挂载触发 0.18s 落定动画。
- **展厅氛围音**：`audio.ts` 的 `setAmbient(chapter)` 用 WebAudio 合成每展厅一个根音的三声部低鸣（0.07Hz 呼吸起伏），随静音开关停启；浏览器手势策略下由首次交互自然激活。
- **展品铭牌**：关卡规则行渲染为黄铜铭牌（`.plate`）。

### 画味系统（`src/game/TileArt.tsx` 的 Decor 层）

每块砖首先是一小幅布面抽象画，玩法信息（黑线/端口/滤片）画在画作之上：

- **P 调色板**：ochre / rust / slate / sage / clay / plum / cream / ink 八色低饱和矿物色。
- **三件套**：`Wash`（径向渐退的柔边晕染，替代硬填充）、`Grain`（feTurbulence 颗粒）、`Vignette`（做旧暗边），每幅必带。
- 11 个 `art` 变体各有独立构图（弧线、斜带、叠圆、断笔、飞白等），描边用略不规则的手绘感路径而非几何直线。
- 自查：`npm run proof:art` 生成 contact sheet（`scripts/art-proof.html` / `.png`），用 Edge 无头截图目检全部变体与玩法叠层。

## 4. 如何加一关

1. 在 `src/game/model.ts` 的 `LEVELS` 里追加一个 `LevelDef`：
   - `entry` / `exit` 用**最终网格**的 `(x, y)` 坐标（出口可以在初始边界之外，见第 15 关）。
   - `initialCells` 每项是 `tileId`、`null`，或 `[tileId, ...overlayIds]`（初始叠放，如 `['C', 'K']`）。
   - `tray` 里的 tile id 开局进入托盘；`trayLabel` 设置后托盘常驻。
2. 在 `scripts/test-rules.mjs` 里补三类断言：
   - **初始不可解**（误读必须真的会卡住）；
   - **预期解可解**（出路必须存在）；
   - **穷举/反例断言**（不推翻假设的所有排列/旋转全部不可解；每个"只差一点"的近解也不可通过）。
3. 跑 `npm run test:rules` 和 `npm run build`。

## 5. 验证状态

`npm run test:rules`：**95/95 通过**。关键不变量举例：

- L1 / L4：从初始局面出发**任意单交换都不可解**（最少 2 步）；全排列解唯一（L1）/ 恰好 2 个（L4 下路绕行）。
- L2：无滤片的所有放置**全部**不可解；竖缝片叠对格也不通（方向即信息）；存在两个解族（顶行路线 / 左列路线），教学含义一致。
- L3：初始即诱饵陷阱（蓝蓝相接却通向画外）；骨架摆对但不显影也不通。
- L5：初始只叠滤片于画外的裂缝砖不可解——必须先把它请回画面。
- L6：初始光流死在 B|G 红蓝缝（诱人的侧路）；不拆侧路只叠滤片不可解。
- L8：初始 B 横躺入口；只旋转（四朝向）不换位全灭，只换位不旋转也不通。
- L11 / L12：同构开局（先修链、再处理深色）——L11 的背叛是"深色可以挪开"，L12 的背叛是"挪开还不够，要换窗口"。
- L7：只补一处断口（两种各测）不可解；滤片方向不对也不通。
- L9：位置×旋转共 1536 种局面，有解率 3%。
- L12：只挪开遮片露出的是**诱饵死路**，必须换窗口。
- L15 / L17：未扩展时**全部**不可解；扩展后所有解都必须用到新列。
- L16：720 种排列中仅 6 个解。
- L18：三种操作缺任意一种都不可解。

`npm run analyze`（BFS 最短解步数）：**18 关全部 ≥2 步**（仅 L2 为 1 步，但含"两片滤片二选一"的决策点），终章 3 步复合操作，难度曲线单调平缓。

## 6. 设计禁区（实现层遵守）

- 无倒计时、无连败惩罚、无屏幕抖动。
- "忍住不舒服"永远不是唯一解——每关的出路都是重新分类。
- 过关语只陈述机制（`winLine`），不做心理学解说。
- 颜色是注意力与归组的材料，不是情绪标签。

## 7. 文件结构

```
src/game/model.ts    引擎：类型、五章十八关数据、连通性求解（并查集）
src/game/TileArt.tsx 渲染：画味系统（P 调色板 + 晕染/颗粒/做旧，11 个构图变体）+ 金线/亮芯/端口/残端/负空间/遮片/窗口
src/game/audio.ts    音效：WebAudio 合成（连通/旋转/拉开/过关琶音/盖章/氛围低鸣），可静音
src/App.tsx          交互：拖拽/点选/旋转/拉开边界、展品墙选关、教学手势、盖章演出、深链 #/play/N、localStorage 存档
src/App.css          画廊主题：暗色展厅墙 + 画框/卡纸 + 氛围光 + 光源菱/目标菱/朱砂印/教学气泡/画廊墙
scripts/test-rules.mjs       规则穷举测试（npm run test:rules）
scripts/analyze-difficulty.mjs  BFS 难度分析（npm run analyze）
scripts/art-proof.mjs          画作样张生成器（npm run proof:art → scripts/art-proof.png）
```
